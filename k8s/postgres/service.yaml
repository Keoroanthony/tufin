apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: tufin-policy
  labels:
    app: postgres
    app.kubernetes.io/part-of: tufin-policy
    app.kubernetes.io/component: database
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
